# FieldLink kiosk: NetworkManager starts dnsmasq with this directory for
# "shared" connections, i.e. only while the FieldLink-XXXX setup hotspot is up.
# Every name resolves to the Pi, so a phone's captive-portal check lands on the
# setup page (http://10.42.0.1/) and the phone opens it by itself.
address=/#/10.42.0.1
