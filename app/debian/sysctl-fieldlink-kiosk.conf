# FieldLink kiosk: the display app (unprivileged "kiosk" user) serves the
# Wi-Fi setup page on port 80 while its hotspot is up. Captive-portal probes
# only look at port 80, so it must be that port.
net.ipv4.ip_unprivileged_port_start = 80
